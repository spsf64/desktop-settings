pre_install() {
	if [ -d /etc/skel ]; then
         rm -rf /etc/skel/
         mkdir /etc/skel/
	fi
}

pre_upgrade() {
    pre_install
}

post_install() {
	if [ -d /etc/bash ]; then
		 \cp -r /etc/bash/. /etc/skel
	fi
}

post_upgrade() {
    post_install
}

post_remove() {
    post_install
}
